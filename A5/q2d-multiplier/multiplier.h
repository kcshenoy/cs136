// SEASHELL_OFF_READONLY

#include "binary.h"

// PARALLEL_MULTIPLIER(a, b) implements a binary parallel multiplier. It returns
//   the product of a and b.
struct binary PARALLEL_MULTIPLIER(const struct binary *a, const struct binary *b);